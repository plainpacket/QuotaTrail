<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- WorkManager contributes this permission, but this app never runs a foreground service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <!-- Optional: lets the user exempt the app from battery optimization so Doze defers periodic
         refresh less aggressively. Never forced; only offered from Settings when not yet ignored. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".QuotaTrailApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuotaTrail"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".QuotaTrailActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".surfaces.widget.QuotaTrailWidgetSetupActivity"
            android:exported="true"
            android:theme="@style/Theme.QuotaTrail.WidgetConfigurationDialog" />

        <receiver
            android:name=".surfaces.notification.DeviceCodeLoginCopyCodeReceiver"
            android:exported="false" />

        <receiver
            android:name=".surfaces.notification.StatusNotificationRefreshReceiver"
            android:exported="false" />

        <receiver
            android:name=".surfaces.widget.WidgetRefreshReceiver"
            android:exported="false" />

        <receiver
            android:name=".surfaces.widget.WidgetPackageUpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".surfaces.widget.QuotaTrailWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quotatrail_widget_info" />
        </receiver>
    </application>
</manifest>
